PORT=5000
MONGODB_URI=mongodb://localhost:27017/bloodlink
JWT_SECRET=your_jwt_secret_here
JWT_EXPIRES_IN=7d

# Frontend URL (Used for reset password emails)
CLIENT_URL=http://localhost:3000

# Gmail SMTP Configuration for Password Reset
SMTP_HOST=smtp.gmail.com
SMTP_PORT=465
SMTP_SECURE=true
SMTP_USER=
SMTP_PASS=
MAIL_FROM="BloodLink < >"
